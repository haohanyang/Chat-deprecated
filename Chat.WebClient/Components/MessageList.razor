@using Chat.Common.DTOs;

<ul class="m-0 p-0">
    @foreach (var message in Messages)
    {
        <Message Text=@message.Content Time=@message.Time />
    }
</ul>

@code {
    [Parameter]
    public IEnumerable<MessageDTO> Messages { get; set; } = Enumerable.Empty<MessageDTO>();
}